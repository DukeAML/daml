// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyCld3vofxEDmLKA2rr755AZfRixFl1s010',
    authDomain: 'daml-site.firebaseapp.com',
    databaseURL: 'https://daml-site.firebaseio.com',
    projectId: 'daml-site',
    storageBucket: '',
    messagingSenderId: '311605104014',
    appId: '1:311605104014:web:85956e109fde21ce05e8df'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
